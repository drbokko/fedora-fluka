Docked Fedora for running (and maybe building) Fluka
========================================================
dr.vittorio.boccone@ieee.org
vittorio.boccone@dectris.com

Build your personal container based on drbokko/fedora_27-fluka:
> cd base_container
> docker build -t my_fedora_27-fluka . 

Run image (w/o mount point volumes):
> docker run --rm -i -t my_fedora_27-fluka bash

Run image with volumes and X11 passthough 
> docker run -i --net=host --env="DISPLAY" --volume="$HOME/.Xauthority:/root/.Xauthority:rw" -v $(pwd):/local_path -t my_fedora_27-fluka bash
